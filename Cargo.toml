[package]
# NOTE: the name "rump" is taken on crates.io (an unrelated CLI tool). To
# publish there, rename the package (e.g. "rump-mp") and keep `[lib] name =
# "rump"` so downstream code still says `use rump::...` — the same pattern the
# cryptography-rs package uses for its `cryptography` lib.
name = "rump"
version = "0.1.0"
edition = "2021"
license = "BSD-2-Clause"
authors = ["Darrell Long"]
description = "Multiprecision integer arithmetic implemented from the literature: Knuth Algorithm D division, Montgomery multiplication with a public Montgomery domain, Karatsuba, and deterministic number theory."
repository = "https://github.com/darrelllong/rump"
keywords = ["bignum", "arithmetic", "montgomery", "number-theory"]
categories = ["mathematics", "algorithms"]

[lib]
name = "rump"
path = "src/lib.rs"

[dependencies]
